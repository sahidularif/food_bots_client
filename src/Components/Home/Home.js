import React from 'react';
import CustomerProfile from '../CustomerProfile/CustomerProfile';

const Home = () => {
    return (
        <div>
            <CustomerProfile/>
        </div>
    );
};

export default Home;